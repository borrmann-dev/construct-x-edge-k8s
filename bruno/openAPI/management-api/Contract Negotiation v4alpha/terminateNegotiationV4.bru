meta {
  name: terminateNegotiationV4
  type: http
  seq: 7
}

post {
  url: {{baseUrl}}/v4alpha/contractnegotiations/:id/terminate
  body: json
  auth: inherit
}

params:path {
  id: 
}

body:json {
  {}
}

script:post-response {
  if (res.status === 200) {
  }
}
