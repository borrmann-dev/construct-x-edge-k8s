meta {
  name: initiateContractNegotiationV4
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/v4alpha/contractnegotiations
  body: json
  auth: inherit
}

body:json {
  {}
}

script:post-response {
  if (res.status === 200) {
    bru.setVar('getNegotiationStateV3_id', res.body.id);
  }
}
