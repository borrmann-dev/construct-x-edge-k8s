meta {
  name: initiateTransferProcessV4
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/v4alpha/transferprocesses
  body: json
  auth: inherit
}

body:json {
  {}
}

script:post-response {
  if (res.status === 200) {
    bru.setVar('getTransferProcessStateV3_id', res.body.id);
  }
}
