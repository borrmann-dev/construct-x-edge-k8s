meta {
  name: terminateTransferProcessV4
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/v4alpha/transferprocesses/:id/terminate
  body: json
  auth: inherit
}

params:path {
  id: 
}

body:json {
  {}
}

script:post-response {
  if (res.status === 204) {
  }
}
